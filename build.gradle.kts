repositories {
    maven {
        url "https://artifactory.example.com/libs-release-local"
        credentials {
            username = "admin_user"
            password = "P@ssw0rd123!"
        }
    }
}
